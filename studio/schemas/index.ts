import { post } from './post';
import { localeString } from './localeString';
import { localeText } from './localeText';
import { photo } from './photo';

export const schemaTypes = [post, localeString, localeText, photo];
